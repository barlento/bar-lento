# Bar Lento — staff schedule site

Live site: **https://bar-lento.vercel.app** (Vercel project `bar-lento`, team "BAR LENTO", auto-deploys from `main`, ~60–90 s).
Owner: Bar Lento, 158 8th Avenue, New York (info@barlentony.com). Floor manager: **Marta** (not technical — she edits from the site, never from code).
The owner writes in Italian; answer him in Italian. Everything staff sees on the site is in **English** (the team is mixed Italian/American). The IT/EN toggle exists only inside manager mode and must translate EVERYTHING the manager sees (header, hero, tabs, day/month names, cards, time clock, ranking, personal panels): use `t()`/`data-t` and the `L_EN`/`L_IT` day-month tables for any new text. The choice is per device (`bl_lang`), applied only while logged in as manager, English again on logout, restored on login. "Copy as text" stays English (it goes to the team).

## How to publish a change (the only job that needs code)
Edit the files, commit, `git push origin main`. That's it — Vercel builds and publishes automatically. No build step, no npm install locally (Vercel installs `web-push`).
Push directly to `main` (no PR needed) unless the owner asks otherwise. If a direct push is refused, push a branch and tell the owner to tap **Create PR → Merge**.
Verify after ~90 s at https://bar-lento.vercel.app (add `?v=<n>` to bypass caches). `*.vercel.app` may be unreachable from the sandbox network — say so instead of guessing.

## Golden rules from the owner
- **Simplicity first** ("più semplice e veloce"). One obvious button beats three clever ones. Marta must never need Claude.
- **Look (owner's decision 2026-09-19, "serious, Apple-like, sellable product")**: light grey page `#F5F5F7`, white cards, hairline borders `#E5E5EA`, near-black text `#1D1D1F`, cool greys, ONE blue for actions `#0071E3`; green/red only as semantic status; Inter everywhere (headings 600, tight tracking), IBM Plex Mono for times; **no emoji anywhere in the UI** (labels are plain words, ✓/✕/›/⋯ glyphs only), neutral black avatars, no sway/bounce, **confetti ONLY for the birthday popup** (the one playful thing the owner wants kept, 2026-09-19: confetti with the name of the person in the middle; never for rankings or recaps), hairline shadows, blurred backdrop behind sheets. Tokens in `:root`, overrides at the end of `<style>`. Opening **splash** (`#splash`): "Bar Lento" white serif (Newsreader, only place it is used) on near-black `#141311`, ~3 s then fade, tap to skip, skipped on the silent auto-update reload (`sessionStorage bl_skip_splash`). Toasts at the TOP. Button labels stay on one line: keep them short.
- Light, readable theme; must look great on iPhone (installable PWA, home-screen app) and on PC. Playful "alive" effects: pulsing LEDs, rising cards, confetti — never at the cost of clarity.
- Personal access is mandatory: the schedule is blurred behind "Who are you?" until a person enters their PIN (or the manager logs in). Each person can confirm ONLY their own shifts (server-checked), via a two-step popup (tap shift → tap Confirm).
- Notifications: ONLY (a) "new week posted" and (b) one batched summary of changes that affect employees (shift added/removed/time changed, day closed/opened). Never spam. Quiet hours 11 PM–10 AM New York.
- No public comparisons between employees (Marta's request): personal stats only for staff, rankings only in Manager mode. Exception decided by the owner: the **⏱ Time clock** (who punched in/out, times, past days) stays visible to every signed-in employee — it is operational info, not a ranking. Don't restrict it.
- **Toast is read-only, always.** Clock-ins/outs happen ONLY on the real Toast POS terminal with the employee's own account. The site just mirrors them (history, confirmation, a nice extra to check from home). Never add a way to punch in/out, edit or create time entries from the site — for anyone, manager included. **One exception decided by the owner (2026-09-19)**: anyone on staff who does NOT exist in Toast (added by hand: temporary jobs, e.g. Pietro) clocks in from the app **automatically**, no switch (`punch.appClockNames(data)` = staff names with no Toast link, plus the optional explicit `data.appClock[]`; if Toast is unreachable only the explicit list counts so nobody on Toast ever gets a clock button by mistake). Their email is typed once at the House Rules and reused for everything after, like Toast people. The person sheet shows a read-only "Clocks in from the app (not in Toast)" card with the month list. A person WHO IS in Toast but is salaried and never clocks in there (Marta) can be switched by the manager to the app clock from the person sheet ("Use the app" / "Back to Toast" → explicit `data.appClock[]`); Marta is otherwise a normal employee (PIN, rules, documents, weekly report). Only that person gets a **Clock in / Clock out** button in their strip (instead of My stats) → confirm sheet → `POST /api/me {action:"punch", on}`; `lib/punch.js` keeps one open entry per person in `barlento:punch:<name>` (never edited/deleted; clock-out later than 20 h auto-closes at +20 h and is marked `auto`; removal archives to `barlento:punch_archive`). Their entries flow into the same places as Toast ones: My week hours (`hours` → `appClock:true`), the Time clock for everyone (`clock`, `app:true`), today's badges (`today`), and the manager's month list in the person sheet (`punches&month=YYYY-MM&name=`: date, in → out, duration, month total). Toast people are untouched.
- **Email rule (owner's decision 2026-09-19)**: the person's email is the one in Toast; the app never asks for another. If it changes in Toast, the app follows automatically: weekly reports and re-sent copies go to the CURRENT Toast email (signed email only as fallback; the signed record itself is never altered) and the owner's weekly summary lists who changed it. Manual (non-Toast) people type theirs once at the House Rules.
- **Signing rule (owner's decision 2026-09-19, applies to EVERY current and future document)**: onboarding is name → PIN → read → tick → Sign, nothing else. The person never types their email (it comes from Toast; the House Rules field is only prefilled and confirmed once, every later document reuses it). Every signature always produces TWO emails: the signed copy to the person and a "X acknowledged …" proof email to the owner (`copyTo`) — that proof is the legal protection, never drop it. Never add extra steps, extra fields or extra popups to this flow; a new policy is just one more entry in `documents.js` and rides the same packet/Sign flow.
- Past weeks are a locked legal archive: never editable/deletable; removing a person deletes only their **upcoming** shifts.
- **Ownership notice (owner's request 2026-09-19)**: the app is the exclusive property of Simone Viola; footer line "© 2026 Simone Viola · Bar Lento staff app · All rights reserved · Legal, ownership and privacy" on the app and on every printable page, `legal.html` (ownership, no copying/reselling, confidentiality, plain-language privacy: what is stored, why, who sees, how long, where — Vercel, Upstash, Resend, Toast read-only, Google Fonts — and how to ask for information, licensing or purchase: simoneviola@barlentony.com), `LICENSE` in the repo. Employees sign nothing for this; it is informational. Keep the contact email there.
- Manager password lives only in Vercel env `admin_password` (any case). Never print secrets in chat, code or logs.

## Architecture (vanilla, no framework)
- `index.html` — the whole UI (staff + manager) in one file: CSS, i18n dict `I18N.en/it` (`t()`, `data-t`), IIFE app. Polls `/api/data` every 45 s. Service worker `sw.js` (push display only, no caching). PWA: `manifest.webmanifest`, `icons/`.
- `api/*.js` — Vercel serverless functions (Node, CommonJS):
  `data` (GET schedule / POST save with optimistic `version`, 409 on conflict; removing a staff name also wipes their PIN + devices), `login`, `confirm`, `log`, `export` (Excel workbooks: schedule archive, personnel record), `push` (subscriptions), `notify` (manager flush), `toast` (POS: `status`, `today`, `clock`, `employees`, `who`), `me` (personal access: `begin`/`create`/`login`/`logout`, `who`, `hours&week=` per-person Toast entries, manager `list`/`reset`, app clock `punch`/`punches`).
- `lib/store.js` — Upstash Redis via REST (env prefix auto-detected KV_/UPSTASH_REDIS_/STORAGE_/REDIS_); seed fallback `data.json`. Keys: `barlento:schedule`, `barlento:confirm`, `barlento:log`, `barlento:pending*`, `barlento:announce`, `barlento:push`, `barlento:toast:*`, `barlento:pin` (name → scrypt hash), `barlento:sessions` (device token → name), `barlento:rules_ack` / `barlento:rules_ack_archive` (House Rules acknowledgments, legal record).
- `lib/accounts.js` — staff PIN accounts: 4 digits, scrypt+salt, 5 wrong tries → growing lock (5→60 min), random 48-hex device token in header `x-staff-token` (localStorage `bl_me_token`/`bl_me_name`), `resetPin` = clear PIN + sign out everywhere.
- `lib/diff.js` — computes changes between saves → history log + "notable" changes for the batched push.
- `lib/push.js` — Web Push (VAPID; public key in file, private in env `VAPID_PRIVATE_KEY`), `flushPending` with quiet hours.
- `lib/mail.js` — outgoing email for the signed copy of the House Rules: SMTP via nodemailer (`SMTP_HOST/PORT/USER/PASS`, e.g. Google Workspace app password) or Resend (`RESEND_API_KEY`), `MAIL_FROM` optional, `MAIL_WEBHOOK_URL` for local tests. `enabled()` → `features.mail`. **LIVE via Resend** since 2026-09-18: `RESEND_API_KEY` + `MAIL_FROM` on Vercel, domain barlentony.com verified (DKIM TXT `resend._domainkey` + CNAME `rsend`/`send` on Squarespace DNS; Google MX/SPF/DKIM untouched, receiving stays OFF). If not configured the acknowledgment is still recorded; Marta can re-send later with the ✉ button in Staff.
- `lib/toast.js` — Toast POS Standard API (read-only): login, restaurant auto-discovery, employees (paginated), time entries, `dayStatus`, `autoMap` (fuzzy first-name matching so nobody maps names by hand). Env: `TOAST_CLIENT_ID`, `TOAST_CLIENT_SECRET` (optional `TOAST_RESTAURANT_GUID`).
- `documents.js` — staff policies (see 📂 Documents below), `docs.html` printable versions.
- `lib/former.js` — register of people no longer on staff (`barlento:former`: name, Toast guid, full name, email, removedAt, by manager/toast), written by both removal paths, never deleted; manager `list` returns `formerStaff`. **Reports are proof of ACTUAL work (owner's rule 2026-09-19)**: every report is built from clock-ins (Toast POS or app); the published schedule is a plan and is never presented as hours worked (manager report lists it after the clock-ins under "Scheduled shifts (the plan, not proof of work)"). Nothing about money or tips, ever. **Employee's own report** (`GET /api/export?mine=1&from&to`, `x-staff-token`, default = current schedule week, max 12 months; My week → **Week report** button = the week shown): hours worked, clock-ins, days worked, every clock-in with exact times and "Recorded by", totals by day, a plain-language note — no schedule, no documents, no manager fields. **Sunday-night email** (`api/weekly.js`, Vercel cron `0 4 * * 1` UTC = Sunday 23:00 EST / midnight EDT; manager can rerun with `?week=<Monday>&force=1` + password; one send per week via `barlento:weekly_sent:<week>`; cron auth = `CRON_SECRET` bearer if set, else Vercel's cron headers): every person on staff with clock-ins that week gets "Your Bar Lento week — …" with hours worked and their PDF attached (email = the one they signed with, else Toast email; test accounts and people without email or without clock-ins are skipped), plus ONE summary email to `copyTo` listing sent/skipped/failed, and a history line. `lib/mail.send` accepts `attachments`. **Personnel record (manager)**: `GET /api/export?person=Name[&from&to]` (password) → an official **PDF** by default (`lib/pdf.js` on `pdfkit`, US Letter, Helvetica, Apple-like: kicker + title, five number tiles, key/values, month-by-month, then Shifts / Clock-ins / Documents / Staff events tables with repeated headers, Certification paragraph citing NY Labor Law §195(4), §661, 12 NYCRR §146-2.1, NYC Admin Code §20-919, Labor Law §201-g; footer on every page with generation time, page x of y and the report's SHA-256 fingerprint; PDF permissions: printing allowed, editing disabled). `&format=xlsx` gives the Excel workbook instead (`exceljs`): sheets **Summary** (identity, status, period, totals, month-by-month table), **Shifts**, **Clock-ins** (Toast POS + app, source column, fetched month by month by guid, works for archived employees), **Documents** (every signed policy incl. older versions/archives: version, email, device, SHA-256), **Events** (staff changes, exact-name match). Bold grey headers, frozen header row, autofilter, real date cells, hours as numbers, print footer. `GET /api/export` (no person) = schedule archive, PDF by default (one table per week, day notes inline) or `&format=xlsx` (Summary, Shifts, Days). Default period 12 months, max 24. Buttons: person sheet "Download PDF", the **Former staff** list at the bottom of Staff, and the manager bar "Export PDF". Preview PDFs locally with `node tools/pdfshot.js file.pdf out/prefix` (pdf.js in headless Chromium). Nothing is ever deleted on removal: past shifts stay in `weeks`, acks → archives, punches → archive, Toast link → former register.
- `lib/backfill.js` — **Past weeks from Toast** (owner's request 2026-09-19): manager → Staff → "Import past clock-ins from Toast" (`POST /api/data {action:"backfillToast"[, from]}`, default 12 months back, idempotent): for every closed clock-in in Toast (bar day, person linked now or in the former register) it creates the past week if missing and adds the person's shift with the ACTUAL in/out times, `src:"toast"` (kept by `normalizeShift`), never over a manager-written shift, never in the current/future weeks, history line. In the app such shifts show a "Toast" tag instead of a station. In reports they are never listed as "Scheduled shifts" (not a plan): the clock-ins themselves are the proof; the schedule archive marks them station "Toast" / "clock-in from Toast". Marta is salaried and does not clock in: her days are entered by hand once the owner gives her hours (never invent times).
- `lib/staffsync.js` — **Staff follows Toast** (owner's request 2026-09-19): on every GET `/api/data` (max once per 10 min, never within 90 s of a manager save) every active Toast employee not yet linked becomes a person in `staff` (first name; "First L." if taken; exact `toastMap` link so the Toast email prefills the signature), log line "Added from Toast: …", the manager sees a toast and the Staff lead says so; two Toast records with the same first name as an unlinked existing person → the older `createdDate` is the existing one; archived (`deleted`) employees are skipped. Removing a person puts their guid in `toastIgnore` (server-side in `api/data` POST) so they never come back. **Archived in Toast → removed from the app automatically** (owner's rule 2026-09-19): on the same sync, any staff name with an explicit `toastMap` link to an employee whose record is archived (`deleted`, or a real `deletedDate`; Toast writes `1970-01-01` for "never") is removed like the manager's Remove (upcoming shifts deleted, past weeks untouched, PIN/devices closed, signatures and clock-ins archived, log line "Removed from staff (archived in Toast)"), NOT added to `toastIgnore` (re-activated in Toast → comes back); a guid missing from Toast's answer never removes anyone. People added by hand (no link) are managed only in the app. Toast test accounts (`Test …`) are never added. Opening Staff (manager `list`) syncs right now with a fresh employee list (`force`), the roster shows a grey line "Toast: N active employees · Not in the app: X (removed here / waiting)" (`toastReport`) so nobody wonders why someone is missing. Person sheet shows "Toast · Full name · email" (manager `list` returns `toast`). Nobody types names or emails twice.
- Data model: `{staff[], birthdays{name:YYYY-MM-DD}, toastMap{name:guid}, toastIgnore[guid], weeks{<MondayISO>:{mon..sun:[{id,name,station,start,end}], notes:{day:{status:''|closed|holiday|half|event,text,open?,close?}}}}}`.

## Features already live (don't rebuild — extend)
Weekly view with tabs; today hero with LED whose headline states the reason automatically from Day settings / the US holiday calendar ("Closed for Christmas Day", "Closed for Staff party", "Open now · Private event", "Open now · Half day"; plain days "Open now" / "Closed now" / "Closed today") — owner's rule 2026-09-19, never typed by hand; typed confirmation; manager mode (add/edit/delete shifts, staff + birthdays with confetti popup, day closures with presets incl. half-day hours, US holidays auto-detected, one-tap **New week** (copy last / empty), delete current/future week with double confirm, change history, CSV export, Notify team, IT/EN); push notifications with bell button; new-week popup once per device; **⏱ Time clock** button for everyone (Toast clock-ins/outs, day navigation, live counter) and clock badges on today's shifts.
**Personal access (name + PIN)**: first visit → "Who are you?" name grid → create/enter 4-digit PIN on a big keypad → device remembered (token). Header button 👤 + strip "Hi Marco · Next shift…" with two buttons, own shifts tagged YOU. **📅 My week**: week nav, tiles scheduled / worked (Toast) / difference, per-shift clock-ins, tap to confirm, "Not you? Switch", "Log out here". **🌟 Week in review** popup once per person per week (`/api/me?action=recap&week=`): last week vs the week before, always encouraging (more hours, punctuality up, early bird, gentle nudge) — never negative. **⭐ My stats** (`/api/toast?action=mystats`, identity from the PIN token, or manager + `&name=`): ONLY own hours, on-time % (after 3 scheduled shifts), streak, badges, encouraging message, anonymous team totals — never other people's numbers or a rank. Manager **Staff**: one clean row per person (avatar, name, status line "PIN ✓ · Rules ✓ · Docs k/5" or "Hasn't signed in yet", 🎂 date, ›); tapping opens the **person sheet** (`#personOverlay`, `openPerson`): birthday, access + **🔑 Reset PIN** (double tap; also signs out their devices), documents with dates + ✉ re-send, **📅 View week**, remove (double tap). **Day settings** = 2×2 tiles (Open / Half day / Private event / Closed — other; + holiday tile when a US holiday): Open/Event/Holiday save on tap, Half day reveals hours, Closed reveals the reason; the status `<select>` is hidden and only drives the logic. **🏆 Team ranking** exists only in Manager mode (`?action=leaderboard`, password required; `lib/leaderboard.js`): Hours + On-time columns, podium, live re-ordering. The manager (Marta) explicitly asked that staff never see comparisons — keep it that way.

**📜 House Rules** (`rules.js` = the text, dual-mode: `window.BL_RULES` in the browser, `require()` on the server; `rules.html` = printable page): shown ONCE per version right after the PIN login, blocking (also for the manager if she signed in with her own name): read to the end → email → checkbox → "I have read and agree" → `POST /api/me {action:"ackRules"}`. Server checks the version against `rules.js`, stores name/email/time/device + sha256 of the text in `barlento:rules_ack` (first acknowledgment of a version wins, older versions kept in `history`), logs a line in the change history. Removing a person moves the record to `barlento:rules_ack_archive` (never deleted; manager `list` returns it as `formerAcks`). On acknowledgment the full text + electronic signature block (Toast full name, email, NY date/time, version, text fingerprint, device) is emailed to the person with a copy to `RULES.copyTo` (simoneviola@barlentony.com); the email field is prefilled with the person's Toast email. Roster shows "Rules ✓ date · email" / "not acknowledged yet" and a ✉ re-send button when mail is configured. Contacts: simoneviola@ / martanicita@ / chiaratinazzi@barlentony.com (no dots). `version: "2026-09-18"` is LIVE; a new version asks everyone again; typo/email fixes may keep the version (the hash proves what was signed). Text is English only, 18 sections, reviewed against NY law by a panel (cashless ban NYC Admin Code §20-840 → manager handles cash; uniform wash-and-wear + enough shirts 12 NYCRR 146-1.7; CROWN Act hair; §201-d cannabis "observable signs"; §52-c monitoring notice wording; §193 no deductions; ESSTA sick/safe leave; §206-c lactation; NLRB-safe privacy/social media wording; workers' comp reporting; off-the-clock work). Owner duties outside the app (told to him): hand out the NY sexual-harassment policy, the sick/safe leave policy + DCWP notice, a written tip policy, the lactation policy; post the §52-c notice and the work phone number in the staff area; confirm cameras record no audio; give one shirt per day worked; decide manager cash float vs cash-to-card kiosk. The owner declined an attorney review.

**📂 Documents** (`documents.js` = the texts, dual-mode like `rules.js`; `docs.html?id=` printable, with the complaint form appended for `harassment`): the legal policies every person reads and signs like the House Rules, one popup at a time right after the rules. Five entries: 🛡 `harassment` (NYS model Sexual Harassment Prevention Policy 2024, adopted in full + Bar Lento reporting summary + complaint form; report to the Owners), 🎓 `training` (annual NYS model video EN/IT, `kind:"training"`, version = calendar year, re-asked every January), 🩺 `pto` (NYC Protected Time Off / safe & sick leave as amended 2026-02-22: ONLY what Toast Payroll is confirmed to do — owner's rule 2026-09-19, "nothing promised that payroll cannot show": 1h per 30h up to 40h paid, carryover 40, 32h unpaid front-loaded, calendar year Jan–Dec, balances on pay stubs, documentation only after >3 consecutive days, no minimum increment; 20h paid prenatal leave is mentioned only in section 10 as a State-law right "arranged with an Owner", NO payroll bank/pay-stub line until Toast confirms the bucket (case 19674799); no "posted in the staff area" claims; `version:"2026-09-19"`; official DCWP Notice EN/IT linked), 💵 `tips` (no pool; card tips via Toast Payroll in full, cash tips kept and declared in Toast, owners/manager never share, §196-d), 🍼 `lactation` (NYS DOL model policy 2024 revision, paid 30-min breaks, space = a private back-of-house area agreed in writing, no dedicated room; `version:"2026", yearly:true` like the training because the law wants it handed out every year). Flow (owner's choice, "one read, one signature"): after the rules, ALL unsigned policies open together as ONE blocking packet "📂 Your documents (4)" (`openPacket`: every text in full, NO email field (the email typed once at the House Rules is reused: `signedEmail()`, shown as "Signing as Name · email"; server `signingEmail()` falls back to the rules/previous ack email), one checkbox, one **Sign** → `POST {action:"ackDocs", ids, versions, email}` → one record per document, ONE email with everything to the person + ONE proof email to the owner, `mail.packetCopy`); only the 🎓 training may wait with **Later** until `trainingDays` (30) after the later of version date / PIN creation (`since`) (one reminder per device per day, `bl_docs_snooze`), then it blocks too. `deadlineDays` (7) is only used for the status text of unsigned policies. Server: `POST /api/me {action:"ackDoc", id, email, version}` (token, first-ack-wins, history, sha256 of the doc JSON, emailed copy to the person + separate "✓ X acknowledged…" proof email to `copyTo`), keys `barlento:doc_ack:<id>` and archive `barlento:doc_ack_archive` on staff removal; `who` returns `docAcks`, `since`, `docsVersions`; manager `list` returns per-person `docs`; `POST {action:"sendCopies", name}` re-sends to the person only (no owner copy again): House Rules + one packet email with every signed document (roster ✉) — only documents whose signed version is still the live one (same text as the fingerprint); `myCopies` is throttled to once per 10 min per person (429). First acknowledgment is written with HSETNX (double tap = one record). UI: My week → **📂 My documents** (badge = how many to sign) lists rules + docs with signing dates, tap to reread/print, **✉ Email me my signed copies** (`POST {action:"myCopies"}`, token, own recorded emails only); roster shows "📂 Documents k/5" + icons. Changing a document's `version` asks everyone again. Owner duties outside the app remain: post the DCWP Notice + NYC CCHR anti-harassment poster in the staff area, make Toast Payroll accrue/print protected time off on pay statements, hand out the LS 54 pay notice at hire, confirm no card fees are deducted from tips.

## Ideas the owner liked (next candidates)
1. Personal follow-ups: personal push + morning-of reminder, add-to-calendar (.ics). (Name+PIN login, My week, My stats are live.)
2. Shift swap / unavailability requests approved by Marta.
4. My stats extras: personal weekly recap push (opt-in), personal bests; manager ranking: monthly export.
5. Weather on day cards (Open-Meteo, no key).
6. Printable week + monthly hours summary for the owner.

## Welcome guide
`welcome.pdf` (root, served at /welcome.pdf, linked in the footer as "Guide") is the team's welcome guide in English, built by `node tools/guide.js` from real screenshots taken by `node tools/guide-shots.js` on the dev server (owner's request 2026-09-19: a beautiful PDF, not a video). It says: made entirely by Simone Viola for the team; what the app is NOT (does not replace Toast clock-ins or payroll); one channel for everything; first time = name, PIN, read, tick, Sign; every week; hours and the Week report + Sunday email; documents; Clock in/out only for people not on Toast; use the Toast email. Birthdays are deliberately NOT mentioned (a surprise). Regenerate it (shots + guide) after UI changes that alter the screens it shows.

## Local testing and the weekly check
`tools/` (excluded from Vercel via `.vercelignore`): `tools/devserver.js` fakes Redis + Toast (+ mail captured at `/__mail`, one Toast employee not on staff, Pietro absent from Toast) on :4173, password `segreta`; `tools/tests/*.test.js` are the Playwright end-to-end suites (ui, docs, rules, mail, it, punch, sync) — run them ALL with `./tools/run-tests.sh` (fresh server each run; `NODE_PATH=/opt/node22/lib/node_modules`); `tools/healthcheck.js` is the read-only production check (every link in the legal texts incl. YouTube via oEmbed, live features on, Toast reachable, time clock, printable pages, live texts = repo versions; exit 1 when something needs attention). **Owner's standing request (2026-09-19): once a week run the health check + the suites, fix what is broken (a dead link, a page that does not render) and push, and tell the owner in Italian only what needs attention.** A Routine fires it every Monday 9:00 New York; keep it alive.
